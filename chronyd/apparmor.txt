#include <tunables/global>

profile chronyd flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability,
  file,
  signal,
  network,

  capability setgid,
  capability setuid,
  capability dac_read_search,
  capability sys_time,

  /entrypoint.sh ix,
  /bin/startup ix,
  /bin/** ix,
  /usr/bin/** ix,
  /sbin/** ix,
  /usr/sbin/chronyd ix,
  /usr/bin/chronyc ix,
  /usr/sbin/su-exec ix,
  /lib/** r,
  /usr/lib/** r,

  /etc/chrony/** rw,
  /etc/chrony-version r,
  /run/chrony/** rw,
  /var/lib/chrony/** rw,
  /data/** rw,
  /tmp/** rwk,
  /etc/TZ r,
  /etc/localtime r,
  /usr/share/zoneinfo/** r,
  owner /data/options.json r,

  network inet dgram,
  network inet6 dgram,

  ptrace (trace,read) peer=docker-default,
}
