#include <tunables/global>

profile netboot_xyz flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability,
  file,
  signal,
  network,

  capability setgid,
  capability setuid,
  capability dac_read_search,
  capability chown,
  capability fowner,

  /bin/** ix,
  /usr/bin/** ix,
  /sbin/** ix,
  /usr/sbin/** ix,
  /lib/** r,
  /usr/lib/** r,

  /start.sh ix,
  /init.sh ix,
  /etc/supervisor.conf r,
  /run/supervisor/** rw,
  /run/** rwk,
  /tmp/** rwk,
  /var/tmp/** rwk,
  /var/log/** rw,

  /app/** r,
  owner /app/** rw,
  /usr/lib/node_modules/** r,

  /config/** rw,
  /defaults/** r,
  /assets/** rw,
  /var/lib/nginx/** rw,
  /var/log/nginx/** rw,
  /etc/nginx/** r,
  /etc/passwd r,
  /etc/group r,
  /etc/TZ r,
  owner /data/options.json r,

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  ptrace (trace,read) peer=docker-default,
}
